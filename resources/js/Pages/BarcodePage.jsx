import { BarcodeScanner } from "@/Components/scanners/Barcode/index";

const BarcodePage = () => {
    return (
        <div>
            <BarcodeScanner />
        </div>
    );
};

export default BarcodePage;

import BasicSection from "@/Components/sections/BasicSection";

const ServiceSection = ({ title, children }) => {
    return <BasicSection title={title}>{children}</BasicSection>;
};

export default ServiceSection;
